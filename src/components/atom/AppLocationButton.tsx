import LocationIcon from "../icons/LocationIcon";

const AppLocationButton = () => {
    return (
        <button>
            <LocationIcon/>
        </button>
    );
};

export default AppLocationButton;
